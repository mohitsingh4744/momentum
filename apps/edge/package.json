{
  "name": "@momentum/edge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "supabase functions serve",
    "build": "supabase functions build",
    "deploy": "supabase functions deploy",
    "test": "deno test --allow-all",
    "test:functions": "deno test --allow-all supabase/functions/**/*.test.ts",
    "type-check": "echo 'Skipping TypeScript check for Deno edge functions'"
  },
  "devDependencies": {
    "supabase": "^1.0.0"
  }
} 