{
  "name": "@momentum/db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "supabase gen types typescript --local > src/types.ts",
    "migrate": "supabase db reset --local",
    "test": "vitest run",
    "db:migrate:dev": "supabase db reset"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.42.6"
  },
  "devDependencies": {
    "supabase": "^1.0.0",
    "vitest": "^1.0.0"
  }
} 